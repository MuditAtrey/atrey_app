# Cloudflare Pages Configuration
name = "atrey-app"

# Pages configuration
[build]
command = ""
cwd = ""
watch_dir = "main"

# The main/ directory contains all static assets
# This is the publish directory for Cloudflare Pages
pages_build_output_dir = "main"
